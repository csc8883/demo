# 无人机巡检视点规划系统 — 点云处理、建模与航点规划技术详细文档

## 目录

1. [系统总览](#1-系统总览)
2. [点云前置处理](#2-点云前置处理)
3. [语义表面建模](#3-语义表面建模)
4. [禁飞区构建](#4-禁飞区构建)
5. [候选航点生成](#5-候选航点生成)
6. [航点规划算法](#6-航点规划算法)
7. [航线生成与安全验证](#7-航线生成与安全验证)
8. [覆盖率评估体系](#8-覆盖率评估体系)
9. [关键配置参数](#9-关键配置参数)
10. [文件索引](#10-文件索引)

---

## 1. 系统总览

### 1.1 数据流架构

```
LAS点云文件 (含语义标签)
    │
    ▼
┌─────────────────────────────────────────────────┐
│  Pretreatment.run()                             │
│  ① 读取 LAS，提取 xyz + classification          │
│  ② build_semantic_surface_model() → 语义体素模型 │
│  ③ generate_candidate_views() → 候选航点         │
│  ④ 输出 _voxel.npz + _candidates.json           │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│  Planner (贪心 / 单层RL / 分层RL)                │
│  输入：体素模型 + 候选航点 + 约束参数             │
│  输出：最优航点子集 (JSON)                       │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│  plan_route_from_waypoints()                    │
│  输入：航点列表 + 体素模型                       │
│  输出：完整飞行航线 (含 A* 绕行、安全调整)       │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│  validate_route_safety()                        │
│  输入：航线 + 体素模型                           │
│  输出：违规报告 (点位/线段/禁飞区)               │
└─────────────────────────────────────────────────┘
```

### 1.2 核心文件职责

| 文件 | 行数 | 职责 |
|---|---|---|
| `planning_core.py` | 4682 | 核心建模、候选生成、覆盖率评估 |
| `planning_solvers.py` | 2493 | 6 种规划算法实现 |
| `route_planner.py` | 2287 | 航线生成、安全验证、A* 绕行 |

---

## 2. 点云前置处理

### 2.1 LAS 文件读取 (`algorithms.py:47-94`)

**函数**: `read_las_for_vis(file_path)`

**处理流程**:

1. 使用 `laspy` 打开 LAS 文件，提取三大数据矩阵：
   - `points` — xyz 坐标 (N×3)
   - `labels` — 语义分类 (N,), 值为 0=导线, 3=地线, 16=杆塔, 22=绝缘子
   - `colors` — RGB 颜色信息

2. **颜色处理**:
   - 若 LAS 包含红绿蓝通道 → 归一化提取（适应 255 和 65535 两种量程）
   - 若无颜色 → 按高度 z 线性插值合成伪彩色（底部→蓝色, 顶部→橙红）
   - 关键设备强制着色：杆塔=红色[1,0.05,0.05], 绝缘子=蓝色[0.05,0.35,1], 导线=金黄[0.98,0.84,0.18], 地线=浅蓝[0.25,0.56,1]

3. **下采样** (当点数 >80000):
   - 优先保留关键标签（杆塔/绝缘子/地线/导线, 最多占 70%）
   - 其余点数随机采样填充，总数控制 ≤80000
   - 返回归一化的中心点坐标 (前端坐标系归零用)

### 2.2 点云体素化与降采样

**函数**: `_voxel_centroids()` (planning_core.py:729-763)

```
输入: N 个点 + N 个标签 + voxel_size
输出: 压缩后的体素质心 + 主导标签
```

**算法**:
1. 计算点云最小包围盒 `min_bound`
2. 对每个原始点 `p`，计算网格索引 `key = floor((p - min_bound) / voxel_size)`
3. 落入同一网格的所有点合并：累加坐标、计数、标签频率统计
4. 每个网格取**主导标签**（出现次数最多的标签）
5. 按点数排序，若 `max_voxels` 限制则截断

### 2.3 安全点构建 (`planning_core.py:766-810`)

**函数**: `build_label_preserving_safety_points()`

**将点云分为两类处理**:

| 类型 | 处理方式 | 目的 |
|---|---|---|
| 安全关键标签 (塔/绝缘子/导线/地线) | **完整保留原始点** | 精确安全约束，不损失关键结构 |
| 非关键标签 (背景等) | 体素化压缩 (cell=voxel_size×10), 最多 60000 体素 | 减少计算量 |

输出带元数据统计（critical_point_count, non_critical_voxel_count, safety_sampling="critical_labels_preserved_non_critical_downsampled"）。

### 2.4 Pretreatment 类 (`algorithms.py:97-154`)

**类**: `Pretreatment(las_path, output_dir, status_key, manual_route_path)`

**`run()` 方法完整流程**:

```
进度 5%  → 读取 LAS (laspy)
进度 30% → build_semantic_surface_model() 构建语义体素
进度 75% → generate_candidate_views() 生成候选航点
进度 100% → 保存 _voxel.npz + _candidates.json
```

**保存的文件**:

| 文件 | 内容 |
|---|---|
| `{name}_voxel.npz` | 体素模型：voxels, attention_targets, display_voxels, wire_curves, conductor_no_fly_volumes, safety_points, z_max_map, local_center, local_frame, meta_json |
| `{name}_candidates.json` | 候选航点列表，每个含 30+ 字段 |

---

## 3. 语义表面建模

### 3.1 主函数: `build_semantic_surface_model()` (planning_core.py:2205-2989)

约 800 行代码，是整个系统最核心的建模函数。

**输入**: `points` (N×3 float), `classes` (N, int), `voxel_size` (默认 0.10m)

**输出**: 含 30+ 字段的完整表面模型字典

### 3.2 建模流程详解

#### 第一步：点云分离

```
全部点云
├── 结构点 (label=16塔 或 22绝缘子)  → 主建模对象
├── 导线点 (label=0)  → _voxel_centroids(voxel_size×5) 压缩至 ≤5000
├── 地线点 (label=3)  → _voxel_centroids(voxel_size×5) 压缩至 ≤5000
└── 安全点  → build_label_preserving_safety_points()
```

#### 第二步：构建局部坐标系 (local_frame)

| 轴 | 确定方式 | 优先级 |
|---|---|---|
| **X 轴** (线路方向) | ① 禁飞区 u_axis → ② 导线 PCA → ③ 杆塔 PCA → ④ [1,0,0] | 依次 fallback |
| **Z 轴** | [0,0,1] (垂直向上) | 固定 |
| **Y 轴** | Z × X (横向) | 右手定则 |

#### 第三步：表面体素提取

1. 将结构点按 `voxel_size` 离散化
2. 计算每个体素的主导标签、质心坐标、26-邻居有无填充
3. **表面判定**：6-邻域（±x,±y,±z）中有任何一个是空的 → 表面体素
4. 绝缘子体素全部视为表面（需要从各个角度拍摄）

#### 第四步：语义分类规则

对每个表面体素（主要针对 tower 体素），按高度和位置分类：

| 语义 | 判定条件 |
|---|---|
| `insulator` | label == 22 (绝缘子) |
| `tower_top` | z_ratio ≥ 0.78（塔上部 22%） |
| `tower_lower30` | z_ratio < 0.40（塔下部 40%, 权重极低 0.05） |
| `tower_edge` | 26-邻居数 ≤10 或 水平径向距离 ≥92%分位数（塔材边缘/外轮廓） |
| `tower_body` | 其余（塔身主体, 权重 1.8） |

**额外属性**:
- `layer_id`: 按 1.5m 高度分层编号
- `sector_id`: 按 30° 方位角分区编号 (0-11)
- `normal_hint`: 从中心向外辐射方向（用于后续 FOV 和入射角计算）

#### 第五步：绝缘子实例提取与合并

**初始聚类**:
1. 绝缘子体素按 26-连通域分组 (`_connected_components`)
2. 每个连通域用 PCA 提取主轴方向
3. 计算中心、长度（沿主轴投影范围）、半径（垂直于主轴 85%分位数）

**两阶段合并** (`_merge_insulator_clusters` + `_merge_clusters_by_proximity`):

```
第一阶段：过滤碎片
  ├── 点数 < 10 → 丢弃
  ├── 点数 < 80 且 长度 < 0.4m → 丢弃
  └── 其余 → 保留为候选

第二阶段：空间聚类合并
  连接条件（同时满足）：
  ├── 中心距离 ≤ 1.5m
  ├── 主轴夹角 ≤ 25°
  └── 外方向(center→tower)夹角 ≤ 30°
  
  合并方式：取平均中心、平均轴、平均半径

第三阶段：若仍超过 80 个
  ├── 按中心点空间接近度再合并
  └── 超过上限则按点数排序截断
```

#### 第六步：关注点生成 (attention_targets)

针对 5 种关键连接部位，自动发现需要特写关注的位置：

| 关注点类型 | 生成方式 | 距离限制 | 最大数量 |
|---|---|---|---|
| **导线端挂点** | 导线点中离绝缘子表面最近的点 | ≤3.5m | 36 |
| **绝缘子横担端挂点** | 绝缘子表面点中离杆塔最近的点 | ≤2.5m | 36 |
| **地线挂点** | 地线点中离塔顶最近的点 | ≤4.5m | 24 |
| **塔基连接** | 按 45° 方位分箱，每箱取离塔中心最远的点 | — | 12 |
| **导线端挂点 (旧别名)** | 同"导线端挂点" | ≤3.5m | 36 |

每个关注点附带：坐标、法线方向（指向对应结构）、权重（connection=6.0, ground_wire_connection=4.5, tower_base=2.0）、最小/最大拍摄距离、最大视角角度、要求分辨率。

#### 第七步：目标面片聚合 (voxels / target_cells)

**塔类目标** — 按 `(semantic, layer_id, sector_id)` 聚合:
- 同语义+同层+同扇区的 display_voxels 合并为一个 patch
- 取平均坐标、平均法线、平均权重、平均面积

**绝缘子目标** — 两种粒度:
1. Patch 级别：按 `(insulator, instance_id, axial_segment_id, around_id)` 聚合（6段×6环=36个面片/实例）
2. Cell 级别 (`target_cells`)：在绝缘子表面按 6 段 × 6 环生成精细采样点，每个点方向为外向+径向组合

**关注点目标**: 每个 attention_target 作为独立目标加入 voxels 列表

#### 第八步：拓扑验证

自动检查三种关键连接类型是否同时存在：
- conductor_insulator_connection（导线-绝缘子）
- insulator_tower_side_connection（绝缘子-杆塔）
- ground_wire_tower_connection（地线-杆塔）

生成 `topology_validation` 字典，包含缺失警告。

### 3.3 输出结构详解

```python
{
    "voxels": [                          # 规划目标面片列表
        {
            "id": int,                   # 唯一ID
            "coord": [x, y, z],          # 中心坐标
            "semantic": "insulator" | "tower_top" | "tower_edge" | "tower_body" | ...,
            "weight": float,             # 语义权重
            "area": float,               # 估算面积
            "normal": [nx, ny, nz],      # 法线方向
            "normal_hint": [nx, ny, nz], # 法线提示(同normal)
            "required_resolution": float, # 要求 GSD (mm/px)
            "incidence_max_deg": float,   # 最大允许入射角
            "min_dist": float,            # 最小拍摄距离
            "max_dist": float,            # 最大拍摄距离
            "max_view_angle_deg": float,  # 最大视角
            "instance_id": int,           # 绝缘子实例ID
            "layer_id": int,              # 层级ID (塔类)
            "sector_id": int,             # 扇区ID (塔类)
            "axial_segment_id": int,      # 轴向段ID (绝缘子)
            "around_id": int,             # 环向ID (绝缘子)
            "point_count": int,           # 支撑点数
            "source": str,                # 来源描述
        },
        ...
    ],
    "attention_targets": [...],          # 关注点列表 (同上结构 + attention特有字段)
    "display_voxels": [...],             # 所有表面体素 (用于3D可视化)
    "target_cells": [...],               # 绝缘子精细采样点
    "wire_curves": [                     # 导线/地线折线模型
        {
            "id": int,
            "type": "conductor" | "ground_wire",
            "polyline": [[x,y,z], ...],  # 平滑折线
            "occ_radius": 0.10,          # 遮挡检测半径
            "safety_radius": 5.0,        # 安全距离
            "point_count": int,
            "source": "point_cloud_pca_polyline",
        },
        ...
    ],
    "conductor_no_fly_volumes": [...],   # 禁飞区棱柱体记录
    "min_bound": [x, y, z],              # 最小包围盒
    "max_bound": [x, y, z],              # 最大包围盒
    "z_max_map": np.ndarray,             # 高度图 (用于遮挡检测)
    "safety_points": np.ndarray,         # 安全约束点 (N×3)
    "safety_labels": np.ndarray,         # 安全标签 (N,)
    "local_center": [x, y, z],           # 局部坐标原点
    "local_frame": {                     # 局部坐标系
        "origin": [x, y, z],
        "x_axis": [x, y, z],             # 线路方向
        "y_axis": [x, y, z],             # 横向
        "z_axis": [0, 0, 1],
        "line_direction_source": str,    # X轴来源说明
    },
    "meta": {                            # 元数据 (40+ 字段)
        "power_model_version": "semantic_geometric_v1",
        "z_min": float,
        "z_max": float,
        "tower_height": float,
        "insulator_instance_count": int,
        "patch_counts": {semantic: count},
        "attention_counts": {semantic: count},
        "topology_validation": {...},
        "wire_curve_count": int,
        "no_fly_volume_count": int,
        "safety_voxel_count": int,
        "patch_scale": {...},            # 各语义空间尺度估算
        "warnings": [...],               # 安全警告列表
    },
}
```

---

## 4. 禁飞区构建

### 4.1 核心模型: `ConductorNoFlyVolume` (planning_core.py:1094-1359)

**几何定义** — 一个包围导线-地线之间空间的定向棱柱体（Oriented Prism）：

```
          顶面 (top plane)
         /─────────────/
        /   地线区域  /         ← 上边界拟合地线点
       /─────────────/
      │              │
      │   禁 飞 区   │   ← 四个侧面围合
      │              │
       \─────────────/
          底面 (bottom plane)    ← 下边界拟合导线点

  U轴 = 线路方向 (沿导线走向)
  V轴 = 横向 (垂直于线路，一侧→另一侧)
```

**数学定义**:

| 边界 | 公式 | 说明 |
|---|---|---|
| 底面 | z = bottom_slope × v + bottom_intercept | 导线最低点拟合平面 |
| 顶面 | z = top_slope × v + top_intercept | 地线最高点拟合平面 |
| 左侧面 | v = left_side_slope × z + left_side_intercept | 可选倾斜 |
| 右侧面 | v = right_side_slope × z + right_side_intercept | 可选倾斜 |
| 前/后 | u ∈ [u_min, u_max] | 沿线路向前/后延伸 |

**关键方法**:

| 方法 | 时间复杂度 | 功能 |
|---|---|---|
| `contains(position)` | O(1) | 判定航点是否在禁飞区内 |
| `clearance(position)` | O(1) | 计算航点到禁飞区边界的最近距离 (负值=在内部) |
| `segment_intersects(a, b)` | O(65) | 65 步采样检测航段是否穿越禁飞区 |
| `local_coordinates(pos)` | O(1) | 世界坐标 → (u, v, z) 局部坐标 |
| `world_position(u, v, z)` | O(1) | 局部坐标 → 世界坐标 |

### 4.2 禁飞区构建流程

#### 4.2.1 从点云构建 (`build_conductor_no_fly_volumes_from_point_cloud()`)

```
导线点 + 地线点
    │
    ▼
_split_conductor_branches()  →  将点云分为两个塔侧分支
    │  方法1：沿线路方向PCA分量正负分割
    │  方法2：对偶聚类 (迭代10轮优化两个方向向量)
    │
    ▼
对每个分支 (共2个)：
    _build_conductor_no_fly_volume()
    ├── 确定原点 = 杆塔中心
    ├── U轴 = 分支主方向 (水平)
    ├── V轴 = 垂直于U轴 (水平)
    ├── 将导线点和地线点投影到 V 轴
    ├── 分为左侧(V小)和右侧(V大)
    ├── 底面平面：fit 导线点的左侧最低+右侧最低
    ├── 顶面平面：fit 地线点的左侧最高+右侧最高
    ├── 左侧面平面：fit 左底+左顶的边线
    ├── 右侧面平面：fit 右底+右顶的边线
    ├── U范围：沿轴延展 + extent_margin(10m), 至少 min_length/2(40m)
    └── one_sided=True (仅塔外方向延伸)
    │
    ▼
若分支构建失败 → fallback：不分分支，整个杆塔一侧建一个volume
```

#### *4.2.2 从人工航线构建 (`build_conductor_no_fly_volumes_from_manual_route()`)*

*提取人工航线中的导线端挂点和地线挂点，按同样逻辑构建。*

#### 4.2.3 分支聚类算法细节 (`_split_conductor_branches()`)

```
目标：将导线点分为左右两簇（对应杆塔两侧的线路）

方法1 (PCA分割):
  - 计算所有点的 PCA 主方向 (水平面)
  - 将点沿主方向投影，分成正负两组
  - 每组用分支轴线推断 (_branch_line_axis)
  - 验证：两分支轴夹角必须 >45°

方法2 (对偶聚类, 方法1失败时):
  - 从水平面单位方向向量中找最相反的一对 (dot最小)
  - 初始中心 = 这对方向
  - 迭代分配+更新中心 (最多10轮)
  - 收敛判定：中心变化 <1e-4
```

### 4.3 安全约束层级

系统使用**三级安全模型**（优先级从高到低）:

| 优先级 | 模型 | 来源 | 使用场景 |
|---|---|---|---|
| 1 (最高) | **ConductorNoFlyVolume** | 禁飞区定向棱柱 | 候选过滤 (contains + clearance) |
| 2 | **Wire Curves** | 导线/地线折线 | 候选过滤 fallback + 遮挡检测 |
| 3 | **Safety Points** | 原始安全关键点 | 细粒度距离查询 |

### 4.4 加载与回退机制 (`PlanningEnvironment.__init__()`)

```
加载 _voxel.npz
    │
    ├── 优先: 使用文件中的 conductor_no_fly_volumes
    │
    ├── 若无禁飞区 或 禁飞区来自 manual_route:
    │      从 display_voxels 中提取导线/地线坐标
    │      重新调用 build_conductor_no_fly_volumes_from_point_cloud()
    │      生成的 volumes 替代原有
    │
    └── 若重建也失败: 使用空列表，退化为 wire_curves 防护
```

---

## 5. 候选航点生成

### 5.1 主函数: `generate_candidate_views()` (planning_core.py:2992-3899)

约 900 行代码，从表面模型生成候选航点列表。每个候选航点代表无人机在 3D 空间中的一个潜在拍摄位置。

**输入**: 语义表面模型字典 + 可选的人工航线路径

**输出**: 候选航点列表 (每个含 30+ 字段)

### 5.2 生成策略总览

| 策略 | 目标语义 | 生成方式 | 数量控制 |
|---|---|---|---|
| **AimType 驱动** | tower_overview, tower_top, tower_body | 按预设距离、方位、高度生成环形候选 | 每语义 4-12 个 |
| **语义表面驱动** | tower_body/edge/top/lower30, insulator, attention | 每个 target voxel 按 PATCH_CANDIDATE_CONFIG 生成多方向候选 | 数千 → 过滤后数百 |
| **绝缘子实例驱动** | insulator | 每实例 5 方向 × 3 距离 × 3 镜头 | 每实例 ≤30 |
| **禁飞区外侧补充** | insulator, connection | 在禁飞区横向边界外侧生成替代视角 | 按需 |
| **手动航线注入** | 人工航线的拍摄点 | 直接转换人工航线航点为候选 | 按文件 |

### 5.3 AimType 驱动生成（塔全貌/塔头/塔身）

按 `AIMTYPE_VIEW_PROFILE` 配置的物理相机参数生成：

```
tower_overview (塔全貌):
  距离: 50m (优选), [40-60m]
  俯角: -33° (FOV包含全塔)
  焦距: 24-35mm (广角)
  方位: 3个 (0°/120°/240°)
  高度: 2个 (50%/70%塔高)
  → 最多 6 候选
  回退: 若通过安全过滤的<3个，加 60°/180°/300° 三方向

tower_top (塔头):
  距离: 15m, [10-22m]
  俯角: -14°
  焦距: 35-84mm
  方位: 6个 (每60°)
  → 6 候选

tower_body (塔身):
  距离: 18m, [12-28m]
  俯角: -25°
  焦距: 35-84mm
  高度: 3层 (35%/55%/75%塔高)
  方位: 4个/层 (每90°)
  → 12 候选
```

### 5.4 语义表面驱动生成

每个 target voxel 遍历 `PATCH_CANDIDATE_CONFIG` 的组合：

```
生成公式：position = target_center + radius × outward_direction + z_offset

对每个语义 (insulator/tower_top/tower_edge/tower_body/tower_lower30):
  radii: 多个拍摄距离 (如 insulator: 5.5, 7.5m)
  z_offsets: 多个高度微调 (如 -0.5, 0, +0.5m)
  focals: 镜头级别 (F1=48mm / F2=84mm)
  yaw_offsets: 航向角微调 (如 0°, ±10°)
  → 产生数十到数百候选/voxel
```

### 5.5 绝缘子实例驱动生成

基于 `INSULATOR_INSTANCE_VIEW_CONFIG`:

```
5个拍摄方向:
  - outward (正对绝缘子外侧)
  - outward_plus_line (外侧+沿线路偏移)
  - outward_minus_line (外侧-沿线路偏移)
  - outward_plus_z (外侧+上偏移)
  - outward_minus_z (外侧-下偏移)

3个拍摄距离: 4.5, 6.0, 7.5m
3个镜头: F1=48mm, F2=84mm
→ 每实例 ≤30 候选 (INSULATOR_INSTANCE_VIEW_CONFIG.max_candidates_per_instance)
```

### 5.6 安全过滤 (`filter_candidate_by_safety`)

这是候选质量保证的核心，每个候选航点必须通过以下检查：

```
对每个候选位置 cand_pos:

阶段1: 禁飞区检查
  for 每个 ConductorNoFlyVolume:
    if volume.contains(cand_pos):
      if 允许调整 (绝缘子/挂点类):
        尝试最多6步向外推出 (每步0.5m)
        if 仍 inside → 拒绝 (inside_no_fly)
      else:
        拒绝 (inside_no_fly)

阶段2: 禁飞区间距检查
  clearance = volume.clearance(cand_pos)
  要求: clearance >= no_fly_required_clearance_m (默认 6.5m)
  if 不满足:
    if 允许调整:
      向禁飞区外侧推出 (最多8-已用步数, 每步0.5m)
    if 仍 < safety_distance_m (5m) → 拒绝 (clearance)

阶段3: 导线曲线距离检查
  预过滤: 仅检查 bbox 内 (safety_radius 扩展) 的曲线
  for 每条导线/地线 polyline:
    计算 point_to_polyline_distance
    if dist < safety_rad → 拒绝 (wire_curve)

返回: (安全位置, 最小间距, 是否调整, 调整步数, 警告)
```

### 5.7 光学参数计算

每个候选航点自动计算：

```
计算步骤:
1. compute_view_geometry(position, target):
   → Distance, heading (方位角), pitch (俯仰角)
2. 应用 yaw_offset / pitch_offset (微调)
3. choose_focal_length_eq_mm(aim_type, distance):
   - 根据 aim_type 查 AIMTYPE_VIEW_PROFILE
   - 计算满足 required_resolution 的最小焦距
   - 考虑入射角余弦补偿
   - 夹紧到 profile 允许范围 [f_min, f_max]
   - 连接器类型特殊处理 (使用 incidence angle 而非 pitch)
   → 连续焦距值 24.0-84.0mm
4. 映射到最近命名级别 F0/F1/F2
5. 计算 HFOV 和 VFOV
   hfov = 2 * atan(36mm / (2 * focal_mm))
   vfov = 2 * atan(24mm / (2 * focal_mm))
```

### 5.8 候选去重与编号

```
去重键 = (round(x,3), round(y,3), round(z,3), round(heading,2), round(pitch,2), semantic)
已存在的键 → 跳过

position_id: 基于 (round(x,3), round(y,3), round(z,3)) 去重
```

### 5.9 候选航点输出字段

```python
{
    "id": int,                           # 候选ID
    "position_id": int,                  # 位置去重ID
    "utm_position": [x, y, z],           # UTM 坐标
    "ActionName": "photo" | "none",       # 动作类型
    "AimType": str,                       # 拍摄类型 (中文名)
    "look_at": [x, y, z],                # 注视目标点
    "Distance": float,                    # 拍摄距离 (m)
    "heading": float,                     # 方位角 (度, 0-360)
    "pitch": float,                       # 俯仰角 (度, 负=俯视)
    "base_heading": float,                # 原始方位 (无 yaw_offset)
    "yaw_offset_deg": float,              # 航向角微调
    "focal_length_eq_mm": float,          # 35mm等效焦距
    "focal_level": "F0" | "F1" | "F2",   # 焦距级别
    "focal_is_estimated": bool,           # 焦距是否为自动估算
    "focal_source": str | None,           # 焦距来源说明
    "f_eq_mm": float,                     # focal_length_eq_mm 副本
    "hfov_deg": float,                    # 水平视场角
    "vfov_deg": float,                    # 垂直视场角
    "yaw": float,                         # heading 副本 (兼容)
    "semantic_focus": str,                # 目标语义
    "target_center": [x, y, z],           # 目标中心
    "source": str,                        # 候选来源
    "target_id": int,                     # 目标ID
    "instance_id": int | None,            # 绝缘子实例ID
    "layer_id": int | None,               # 层级ID
    "sector_id": int | None,              # 扇区ID
    "weight": float,                      # 权重 (用于覆盖率)
    "required_gsd": None,                 # (已弃用)
    "max_view_angle_deg": float,          # 最大视角约束
    "no_fly_checked": True,               # 是否做了禁飞区检查
    "no_fly_inside": False,               # 是否在禁飞区内
    "no_fly_clearance_m": float,          # 禁飞区最近距离
    "no_fly_required_clearance_m": float, # 要求的禁飞区间距
    "safety_checked": True,               # 是否做了安全检查
    "adjusted_for_no_fly": bool,          # 位置是否被安全过滤调整
    "adjustment_steps": int,              # 调整步数
    "original_position": [x,y,z] | None,  # 调整前的位置
    "safety_warning": str | None,         # 安全警告信息
    "is_coverage_target": bool,           # 是否参与覆盖率计算
    "manual_priority": float,             # 来自人工航线的优先级
    "aim_type": str | None,               # AimType 原文
}
```

---

## 6. 航点规划算法

### 6.1 规划环境: `PlanningEnvironment` (planning_core.py:4109-4352)

加载 `_voxel.npz`，将体素数据转换为规划器可用的向量化数据结构：

```python
env.target_coords     # (T, 3) 规划目标坐标
env.semantics         # (T,)   语义标签
env.weights           # (T,)   语义权重
env.required_resolution # (T,) 要求GSD
env.incidence_max     # (T,)   最大入射角
env.normal_hints      # (T, 3) 法线方向
env.attention_coords  # (A, 3) 关注目标坐标
env.z_max_map         # 高度图 (遮挡检测)
env.safety_index      # VoxelSafetyIndex (安全距离查询)
env.conductor_no_fly_volumes  # 禁飞区列表
```

**覆盖率计算**: `coverage_from_mask(mask)` → 12 个指标
- C_geo: 几何覆盖率
- C_weighted: 加权覆盖率
- C_ins: 绝缘子覆盖率
- C_top: 塔头覆盖率
- C_edge: 塔边缘覆盖率
- C_body: 塔身覆盖率
- tower/insulator 各自的 covered_count / target_count
- uncovered_summary (按语义) + uncovered_voxels 详情

### 6.2 可见性引擎: `VisibilityEngine` (planning_core.py:4367-4530)

使用 LRU 缓存（默认 20000 条）加速重复查询。

**核心函数**: `visible_indices_for_view(position, pitch, yaw, focal_level, f_eq_mm)`

```
六阶段过滤:

1. 距离过滤
   - 计算 position 到所有 target_coords 的欧氏距离
   - 保留 distance ≤ max_visibility_distance_m (60m)

2. 最小距离过滤
   - 保留 distance ≥ focal.min_distance_m (F0=1m, F1=2m, F2=4m)

3. 相机前方过滤
   - 方向向量 dot (target - pos) > 0 → 在相机前方

4. FOV 角度过滤
   - 水平角 ≤ hfov/2
   - 垂直角 ≤ vfov/2

5. 入射角过滤
   - view_direction dot target_normal 的夹角 ≤ incidence_max_deg
   - 确保拍摄角度不会太侧

6. 分辨率过滤
   - observation_resolution(f_eq_mm, distance, incidence_angle) ≥ required_resolution
   - 确保实际 GSD 满足要求

返回满足所有条件的体素索引数组
```

余弦计算用于 FOV 判定（等效于投影在相机平面上的角度）。

### 6.3 三个规划器 (planning_solvers.py)

#### 6.3.1 基类: `BasePlanner` (第 50-1882 行)

提供所有规划器共享的功能：

**候选加载与预处理**:
1. 从 `_candidates.json` 加载候选 (`CandidateViewpoint` dataclass)
2. 预计算每个候选的可见体素索引（调用 `VisibilityEngine`）
3. 按 position_id 建立候选分组
4. 人工航线注入（若提供）
5. 候选排序和池大小限制（最多 2000）

**人工航线集成**:
- 解析人工航线的航点，匹配到对应语义的候选
- 按 `manual_ratio_min/max`（默认 70%）保留人工航线的航点
- 补充不足语义区的候选

**航点构建** (`_build_waypoint_dict`):
- 将选定的候选 ID 组装为最终航点 JSON
- 支持单航点多 shot（不同焦距拍同一位置）
- 自动分配 `focal_level` 和 `action_name`

**辅助航点插入** (`_insert_auxiliary_waypoints`):
- 在安全距离不足的连续航点之间插入过渡点
- 使用 A* 搜索生成避障过渡路径
- 最大 30 个辅助航点

**结果输出**:
- 保存规划结果 JSON（waypoints + coverage + metrics）
- 保存优化日志
- 覆盖率评估 + 指标汇总

#### 6.3.2 贪心规划器: `SemanticWeightedGreedyPlanner` (第 1883-1897 行)

**算法**: 每次选择 `增益 = 新覆盖的加权体素数 / 代价` 最大的候选。

```
while 未达到最大航点数:
    遍历剩余候选:
        计算 gain = Σ(新覆盖体素权重) / shot_count
    选择 gain 最高的候选
    更新 covered_mask
    if 覆盖率达标 (C_geo≥0.90 且 C_weighted≥0.98 且 C_ins≥0.98):
        break
```

#### 6.3.3 单层RL规划器: `SemanticSingleLayerRLPlanner` (第 1899-1972 行)

基于贪心初始化，用 RL 策略精细化航点选择。

#### 6.3.4 分层RL规划器: `SemanticHierarchicalRLPlanner` (第 2127-2208 行)

**两层结构**:
- **高层策略**: 选择需要覆盖的语义区域（insulator/tower_top/tower_edge/tower_body）
- **低层策略**: 在选定的区域内选择最优的候选航点

这种分层方法特别适合杆塔巡检，因为不同语义区域要求不同的拍摄策略。

#### 6.3.5 其他规划器 (第 1974-2125 行)

| 规划器 | 特点 |
|---|---|
| `RandomizedGreedyRepairPlanner` | 随机贪心 + 修复策略 |
| `BeamSetCoverPlanner` | Beam Search 集合覆盖 |
| `ParetoSemanticPlanner` | Pareto 多目标优化 (覆盖率 vs 航点数) |

### 6.4 规划器注册表 (`algorithms.py:192-203`)

```python
MAIN_PLANNER_REGISTRY = {
    "语义加权贪心": SemanticWeightedGreedyPlanner,
    "单层强化学习": SemanticSingleLayerRLPlanner,
    "分层强化学习": HRLSolver,
}

PLANNER_MODE = {
    "greedy":          → SemanticWeightedGreedyPlanner,
    "rl":              → SemanticSingleLayerRLPlanner,
    "hierarchical_rl": → HRLSolver (分层RL),
}
```

---

## 7. 航线生成与安全验证

### 7.1 航线生成: `plan_route_from_waypoints()` (route_planner.py:2201-2287)

**流程**:

```
输入: 航点列表 + 塔信息 + 体素模型路径

1. 提取所有航点和任务目标点 (look_at)

2. 航点排序
   - 默认角度排序 (_angular_order): 按方位角排列
   - 或最近邻排序 (_nearest_order)

3. 路线构建
   for 每对相邻航点 (A → B):
     a. 构建任务段: A → task_point → B (被拍摄目标飞行的走廊)
     b. 构建过渡段: 上一目标到当前 A

4. A* 绕行规划 (_plan_local_astar_segments)
   - 将局部障碍物（导线/地线/禁飞区）网格化
   - 在 3D 网格中搜索从 A 到 B 的安全路径
   - 绕过禁飞区和其他障碍物

5. 安全调整 (_adjust_for_route_constraints)
   - 检查每个段与安全点的距离
   - 不满足间距要求的段，尝试垂直上移 (_adjust_for_clearance)
   - 调整禁飞区内的段 (_adjust_for_no_fly)

6. 平滑 (_smooth_safe_path)
   - 路径点去冗余
   - 保持安全约束的前提下减少航点数

7. 输出完整航线 JSON (含 waypoints + route_items + 安全计算)
```

### 7.2 安全验证: `validate_route_safety()` (route_planner.py:2010-2109)

**检查维度**:

| 检查项 | Task 段阈值 | 辅助段阈值 | 触发违规 |
|---|---|---|---|
| 航点-导线距离 | task_wire_clearance_m (5m) | wire_clearance_m (10m) | point violation |
| 航点-杆塔距离 | task_tower_clearance_m (3m) | tower_clearance_m (6m) | point violation |
| 航点-禁飞区 | no_fly_margin (max clearance) | no_fly_margin | no_fly violation |
| 航段-导线最小距离 | task_wire_clearance_m | wire_clearance_m | segment violation |
| 航段-杆塔最小距离 | task_tower_clearance_m | tower_clearance_m | segment violation |
| 航段穿越禁飞区 | — | — | no_fly segment violation |

**违规报告**:

```python
{
    "total_violations": int,      # 总违规数
    "point_violations": int,      # 航点违规
    "segment_violations": int,    # 航段违规
    "no_fly_violations": int,     # 禁飞区违规
    "min_wire_clearance": float,  # 最小导线间距
    "min_tower_clearance": float, # 最小杆塔间距
    "min_no_fly_clearance": float,# 最小禁飞区间距
    "violations": [
        {
            "type": "point" | "segment",
            "target": "wire" | "tower" | "conductor_no_fly",
            "index": int,          # 违规点/段编号
            "distance_m": float,   # 实际距离
            "threshold_m": float,  # 阈值
        }
    ]
}
```

---

## 8. 覆盖率评估体系

### 8.1 核心函数

**`evaluate_waypoint_coverage()`** (planning_core.py:4562-4615):
- 输入：航点列表 + 体素路径
- 调用 `PlanningEnvironment` + `VisibilityEngine`
- 对每个航点的每个 view，计算可见体素集合
- 聚合 covered_mask (主目标) + attention_mask (关注目标)
- 合并计算完整覆盖率指标

**`compute_waypoint_metrics()`** (planning_core.py:4618-4681):
- 统计航点数、shot 数、路径总长、平均段长
- 高度范围 (avg/min/max)
- 每航点/每 shot 的覆盖率效率
- 焦距使用分布

**`coverage_thresholds_met()`** (planning_core.py:4355-4364):
- 判定覆盖率是否达标（C_body 豁免）

### 8.2 覆盖率指标定义

| 指标 | 公式 | 阈值 | 说明 |
|---|---|---|---|
| `C_geo` | 被覆盖目标数 / 总目标数 | ≥0.90 | 几何覆盖率 |
| `C_weighted` | Σ(覆盖目标权重×面积) / Σ(全部权重×面积) | ≥0.98 | 加权覆盖率 |
| `C_ins` | 绝缘子体素覆盖数 / 绝缘子体素总数 | ≥0.98 | 绝缘子覆盖率 |
| `C_top` | 塔头覆盖数 / 塔头总数 | ≥0.94 | 塔头覆盖率 |
| `C_edge` | 塔边缘覆盖数 / 塔边缘总数 | ≥0.94 | 塔边缘覆盖率 |
| `C_body` | 塔身覆盖数 / 塔身总数 | ≥0.92 (实际豁免) | 塔身覆盖率 |
| `C_connection_attention` | 关注连接点覆盖率 | — | 信息指标 |
| `C_conductor_insulator_connection` | 导线挂点覆盖率 | — | 信息指标 |
| `C_ground_wire_tower_connection` | 地线挂点覆盖率 | — | 信息指标 |
| `C_insulator_tower_side_connection` | 横担挂点覆盖率 | — | 信息指标 |
| `C_tower_base_connection` | 塔基覆盖率 | — | 信息指标 |

**覆盖判定**: 目标体素在航点 FOV 内、满足入射角约束、满足分辨率（GSD）要求 → 视为被覆盖。

### 8.3 单个视点的可见性判定

```
条件 (全部满足才算可见):

① 距离: distance ∈ [focal_min_distance, max_visibility_distance_m]
   F0≥1m, F1≥2m, F2≥4m, max=60m

② 视场角: 水平和垂直方向角都在 FOV 半角内
   |horizontal_angle| ≤ hfov/2
   |vertical_angle| ≤ vfov/2

③ 入射角: 视角与目标法线的夹角 ≤ incidence_max_deg
   insulator ≤45°, tower_body ≤65°, connection ≤50-55°

④ 分辨率: 实际 GSD ≥ required_resolution
   GSD = (f_eq_mm / 24.0) × (8.0 / distance) × cos(incidence_angle)
   insulator ≥1.5 mm/px, tower_body ≥0.7 mm/px, connection ≥1.2-1.3 mm/px
```

---

## 9. 关键配置参数

### 9.1 体素与建模

| 参数 | 值 | 说明 |
|---|---|---|
| `VOXEL_SIZE` | 0.10m | 基础体素大小 |
| `INSULATOR_CLUSTER_MIN_POINTS` | 80 | 绝缘子实例最少点数 |
| `INSULATOR_CLUSTER_MIN_LENGTH_M` | 0.4m | 绝缘子实例最短长度 |
| `INSULATOR_MAX_CANDIDATE_INSTANCES` | 80 | 绝缘子实例数量上限 |
| `MAX_CANDIDATES_TOTAL` | 50000 | 候选总数上限 |

### 9.2 拍摄约束

| 参数 | 值 | 说明 |
|---|---|---|
| `safety_distance_m` | 5.0m | 基础安全距离 |
| `max_visibility_distance_m` | 60.0m | 最大可视距离 |
| `max_total_shots` | 240 | 总拍摄次数上限 |
| `max_shots_per_waypoint` | 3 | 单航点最多拍摄次数 |

### 9.3 禁飞区

| 参数 | 值 | 说明 |
|---|---|---|
| `conductor_no_fly_extent_margin_m` | 10.0m | 沿线路延伸余量 |
| `conductor_no_fly_min_length_m` | 80.0m | 禁飞区最小长度 |
| `conductor_no_fly_exterior_clearance_m` | 6.5m | 禁飞区横向安全距离 |
| `conductor_no_fly_boundary_tolerance_m` | 0.3m | 边界公差 |

### 9.4 分析处理

| 参数 | 值 | 说明 |
|---|---|---|
| `CAMERA_SENSOR_WIDTH_MM` | 36.0mm | 全画幅传感器宽度 |
| `CAMERA_SENSOR_HEIGHT_MM` | 24.0mm | 全画幅传感器高度 |

### 9.5 层级配置

| 参数 | 值 | 说明 |
|---|---|---|
| `tower_layer_height_m` | 1.5m | 塔身分层高度 |
| `tower_sector_count` | 12 | 塔身方位分扇数 |
| `insulator_segments` | 6 | 绝缘子轴向分段数 |
| `insulator_around` | 6 | 绝缘子环向分位数 |

---

## 10. 文件索引

| 文件 | 行数 | 核心内容 |
|---|---|---|
| `waypoint_planning/planning_core.py` | 4682 | 语义建模、禁飞区、候选生成、可见性引擎、覆盖率评估、14 种 AimType 配置、ConductorNoFlyVolume 类、PlanningEnvironment 类、VisibilityEngine 类 |
| `waypoint_planning/planning_solvers.py` | 2493 | BasePlanner 基类、6 种规划器（贪心/单层RL/分层RL/随机修复/BeamSearch/Pareto） |
| `waypoint_planning/route_planner.py` | 2287 | 航线生成 (plan_route_from_waypoints)、安全验证 (validate_route_safety)、A* 3D 绕行、禁飞区航段检测 |
| `waypoint_planning/algorithms.py` | 244 | Pretreatment 类、LAS 读取、规划器注册表、公开 API |
| `waypoint_planning/waypoint_models.py` | ~400 | Pydantic 输入/约束模型、多编码 JSON 读取 |
